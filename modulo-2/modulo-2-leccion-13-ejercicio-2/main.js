'use strict';

console.log('13: ejercicio 2');

const names = ['María', 'Lucía', 'Susana', 'Rocío', 'Inmaculada'];
console.log(names);

const greetings = names.map((name) => `Bienvenida  ${name}`);

console.log(greetings);
